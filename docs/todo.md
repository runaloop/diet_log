# TODO

## Сделано / снято
- [x] База продуктов вынесена из md в SQLite (`data/diet.db`, STRATEGY.md §11, фаза 0).
- [x] Итоги дня / недели / месяца — `scripts/summary.py day|week|month|weektrend`.
- [x] Открывать `today.md` при открытии папки (`.nvim.lua`).
- ~~Миграция на excel~~ — снято решением по SQLite (STRATEGY.md §11).
- [x] `db add` больше не затирает не переданные поля (`scripts/db.py:cmd_add`): в ветке `ON CONFLICT` каждое поле обёрнуто в `COALESCE(excluded.…, product.…)`, `--estimate` держится через отдельный биндинг. Следствие: значение можно исправить, но нельзя обнулить через `db add` — для этого `db q`.
- [x] Квота «добавки» приведена к решению пользователя: `GROUP_QUOTA` 20 → **7**, сноска о расхождении из STRATEGY.md §15 снята, таблица §6 синхронизирована.
- [x] Дрейф групп снят: канон — `обработка`. `SEED_GROUPS` и 61 ссылка в `seed_med_tags.py` переведены на него, пустая группа `сладкое` удалена из живой базы. `оливковое` намеренно остаётся без квоты (§6: дефолтный жир, судится рубрикой качества жира §7) и внесена в `summary.QUOTA_EXEMPT`. Новый сторож `summary.warn_quota_drift()` пишет в stderr, если в каталоге есть группа с продуктами и без квоты.
- [x] `scripts/new_day.py` — «новый день» одним вызовом (`7777c9d`). Замерено на стенде: 383 с → 43 с, 21 вызов инструментов → 2.5, 19 690 сгенерированных токенов → 1 644.
- [x] Добор рациона после каждой записи убран (`888c678`). Замерено: −33 % времени, −48 % генерации на 42 прогонах; корректность сдвинулась на 14 пунктов, что при трёх повторах неразрешимо.
- [x] Git-блок вынесен в `scripts/git_sync.py`, раздел `## Git` из `AGENTS.md` удалён (`ecbaba6`). Замерено на `итоги дня`: 3 вызова → 2, −21 % времени, −37 % генерации.
- [x] Механика префикса `!` убрана (AGENTS.md, `product-lookup.md`, `log.py`). Единственный признак разового блюда — КБЖУ от пользователя: есть `--k/--b/--zh/--u` → строка пишется как названа и мимо каталога. Побочное: ведущий `!` больше не срезается и попадёт в название строки, снимает его нормализация имени (шаг 1).

## Скрипты — баги и дрейф (найдено при ревизии 2026-09-21)
- [ ] Каталог нельзя потрогать в тесте: `scripts/db` всегда открывает `data/diet.db` (`paths.DB_PATH`), переменной окружения для подмены нет — любая проверка пишет в живую базу (поймано 2026-09-21, тестовый продукт пришлось удалять руками). Дать `paths.DB_PATH` читать `DIET_DB` из окружения.
- [ ] `db init` создаёт базу без колонки `avail`, а `db add` / `db find` её требуют (`scripts/db.py:52–82, 108–115, 172, 201`); живая база работает только потому, что колонку добавили вручную — добавить `avail` в `SCHEMA`/`connect()`.
- [ ] `plan_ration.py` берёт режим из двух источников: `config/cycle.md` для слоя углей (`:732`) и H1 дневника для окна дефицита (`parse_plan`, `:94–96`); при расхождении бюджет и угли считаются в разных режимах — оставить один источник (`global_mode()`).
- [ ] `ration_totals.num()` (`scripts/ration_totals.py:33`) без try/except — нечисловая ячейка роняет скрипт.
- [ ] `summary.py day --любой-флаг` падает: `:851` читает `argv[1]` без фильтра флагов.
- [ ] Мёртвый код: `load_goals()['carbs']` (`summary.py:73`), `load_cycle()` anchor/cycle_len/maintenance_idx (`:83, :93`), `status(invert=True)` (`:270`), `paths.TRAINING_TYPES` (`scripts/paths.py:22`).

## Скрипты — следующие шаги
- [ ] `garmin_sync.py apply`: проверен на JSON-фикстуре и живом `--dry-run`; первый боевой прогон («синк гармин», «новый день» с `--back 2 --base`) сверить глазами с выводом `activities`/`fetch`.

### Оптимизация под слабую модель (по замерам стенда 2026-09-22, в порядке отдачи)
Механика везде одна: убрать рассуждение там, где решения нет. Сокращать текст `AGENTS.md`
дальше смысла нет — H1 показал, что две редакции неразличимы, H6 — что упёртые в потолок
прогоны сидели в окне *ниже* чистых.

- [ ] **`log.py`: прощающий разбор и честная ошибка.** Прогон кейса 3: `log.py "помидоры черри:100г"` → «продукт не найден в каталоге», хотя продукт есть, а сломался разбор граммовки. Модель поверила сообщению и ушла искать product-lookup — три лишних вызова. Принимать `продукт:100г` наравне с пробелом; в тексте ошибки печатать, что искалось после разбора.
- [ ] **`log.py`: «как вчера» / «завтрак как вчера»** — флаг копирования строк из другого дневника, плюс дельта к ссылочной порции (`--portion last -100г`). Кейс 9 падает 0 из 6 на обеих руках, кейс 11 («на 100 г меньше обычной порции») даёт вырожденную генерацию без единого вызова инструмента. Единственный пункт списка, который целится в корректность, а не в цену.
- [ ] **Резать то, что печатают скрипты.** «Вывод verbatim» значит, что модель перепечатывает вывод целиком — каждый символ оплачивается дважды. Решить, что из статус-блока и шапки таблицы нужно в каждом ответе (заголовок таблицы при одной новой строке — нет).
- [ ] **`scripts/lookup.py`** — поиск неизвестного продукта одной командой (Exa → кандидаты с КБЖУ → `db add`). Кейс 7 держит медиану 411 с, процедура описана в спеке и решений в ней нет.
- [ ] **Убрать шаг ориентации.** Почти каждый прогон начинается с `ls today.md; ls diaries/; date`, хотя `log.py` сам разрешает симлинк и сам берёт дату. Сказать в инструкции прямо, что проверять не надо.

## Инструкция и доки
- [ ] `docs/CRINGE.md` и критик-контур STRATEGY.md §12/§16 не подключены к `AGENTS.md` — решить, нужен ли контур, и либо подключить (`docs/agent/ration.md`), либо снять из спека.
- [ ] `.claude/settings.json` проекта: `"effortLevel": "low"` перекрывает пользовательский `/effort` (project > user) — оставить осознанно или убрать.
- [ ] После сокращения `AGENTS.md` (2026-09-21, тег `agents-md-80kb` = версия до): замерить `/context` до/после и прогнать сценарии из плана на новой сессии; если качество упало — бисектить по коммитам «AGENTS.md trim, step 1/2/3».
